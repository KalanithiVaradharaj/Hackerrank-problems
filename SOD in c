#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int n,sod=0;
    scanf("%d", &n);
    while(n>0){
        sod += (n%10);
        n /= 10;
    }
    printf("%d",sod);
    return 0;
}
